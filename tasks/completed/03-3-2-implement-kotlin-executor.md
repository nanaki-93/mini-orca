# Task 3.3.2 — Implement Kotlin Executor

## Milestone
Milestone 3: Language-Agnostic Tool Executor

## Description
Create the Kotlin-specific executor.

## Checklist
- [ ] Create `internal/tools/kotlin_executor.go`
- [ ] Implement `FormatCode()` → runs `./gradlew ktlintFormat`
- [ ] Implement `RunTests()` → runs `./gradlew test`
- [ ] Implement `Build()` → runs `./gradlew build`
- [ ] Implement `Lint()` → runs `./gradlew ktlintCheck`

## Dependencies
- Task 3.1.2, Task 3.2.1

## Deliverables
- `internal/tools/kotlin_executor.go`

## Status
- [ ] Not Started
- [ ] In Progress
- [x] Done
